import { AboutScreen } from '@/components/About/AboutScreen'

// The ABOUT route renders the "DIALOGUE" screen — a Persona-style conversation
// with an AI version of Juniel (Gemini chat + ElevenLabs voice), plus the contact
// links and a message form. Replaces the old Ask Me placeholder.
export default function AboutPage() {
  return <AboutScreen />
}
